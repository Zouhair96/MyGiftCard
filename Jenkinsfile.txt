pipeline {
    agent any 
    stages {
        stage('Build') { 
            steps {
                withMaven(maven : 'MAVEN_HOME') {
                    sh 'mvn clean install'
                }
            }
        }
        stage('Test') { 
            steps {
                 withMaven(maven : 'MAVEN_HOME') {
                    sh 'mvn clean install'
                } 
            }
        }
        stage('Deploy') { 
            steps {
                 withMaven(maven : 'MAVEN_HOME') {
                    sh 'mvn clean install'
                } 
            }
        }
    }
}